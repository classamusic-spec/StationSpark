import React from 'react';
import { ProgressScreen } from '@/screens/Progress/ProgressScreen';

/** /badges — the Badge Wall / progress board. */
export default function BadgesRoute() {
  return <ProgressScreen />;
}
